
node_modules
.DS_Store

.env
.env.*
!.env.example

dist/
.netlify/
.vscode/

# Astro's generated types and content cache; regenerated by `pnpm build`/`astro sync`
.astro/

# Claude Code: share settings.json, keep local overrides and scratch worktrees out
.claude/*
!.claude/settings.json

.cursor
.cursorrules

# Enforce pnpm-only: reject npm/yarn lockfiles
package-lock.json
yarn.lock
